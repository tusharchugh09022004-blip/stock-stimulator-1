services:
  - type: web
    name: stock-simulator-api
    env: node
    rootDir: server
    buildCommand: npm install
    startCommand: node fixed_server.js
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
